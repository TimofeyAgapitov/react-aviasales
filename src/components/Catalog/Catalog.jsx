import { Sorting } from '../Sorting';
import { Tickets } from '../Tickets';

export const Catalog = () => {
  return (
    <section className="catalog">
      <Sorting />
      <Tickets />
    </section>
  );
};

